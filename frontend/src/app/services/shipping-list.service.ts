import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
// import { CartDetails } from './shipping-list.interface';

@Injectable({
  providedIn: 'root'
})
export class ShippingListService {
  
  
}
